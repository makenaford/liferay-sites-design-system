# Local development only. `wrangler deploy` never uploads this file — it exists so that
# `pnpm builder:worker` is usable without a Cloudflare tunnel and a real Access login.
#
# The Worker honours this ONLY for requests to localhost, so setting it cannot open a deployed
# Worker even if the variable somehow reaches production. See worker/access.ts.
ACCESS_DEV_OPEN=true
